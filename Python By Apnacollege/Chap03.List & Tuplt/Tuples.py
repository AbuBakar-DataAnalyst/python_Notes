# Tuples built-in data type that immutable(unchangeable)  sequence of value's
# it like a lists & array but we can't change the indux value

tup = (1,2,3,4,5)
print(tup)

# tup [2] = 5 / Not allowed but list allow it

tup = (1,)
print(tup) # if u have onle one value then must use(,)otherwise it takes as a integer not Tuple